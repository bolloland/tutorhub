<h1>TutorLogin#new</h1>

<% if @tutor.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@tutor.errors.count, "error") %> prohibited this tutor from being saved:</h2>

    <ul>
      <% @tutor.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>Enter your username/password,</br>
<%= form_for(:session, url: tutor_login_path) do |f| %>
   
    <%= f.label "username" %>
    <%= f.text_field :username %></br>
    <%= f.label "password" %>
    <%= f.password_field :password %></br>

    <%= f.submit "Access Your Tutor Account" %>
    </br></br>
   
<% end %>

-or-

<%= button_to "Log in with Google", '/auth/google_oauth2'  %>
